name: Push Workflow

on: [push,pull_request]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name : Print message
        run : echo "Hello, Github Action"
  say-bye:
    needs : job1
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run : echo "bye from 2nd job "